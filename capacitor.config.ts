import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Math Plot 3D',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
